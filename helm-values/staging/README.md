# Staging Helm Values

Promotion copies the already-tested immutable image digest from dev. Staging uses moderate capacity, observability signals, and timed canary rollout steps to rehearse production behavior.
