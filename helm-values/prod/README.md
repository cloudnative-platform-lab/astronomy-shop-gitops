# Production Helm Values

Promotion copies the approved staging digest without rebuilding it. Production uses larger resource requests, higher replica counts, observability signals, and longer timed canary rollout steps.
